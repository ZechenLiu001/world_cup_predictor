# Source And Method Notes

## Audience Contract

Report shape follows the executive-report structure:

1. Title
2. Executive summary
3. Key findings with visual evidence
4. Recommended next steps
5. Further questions
6. Caveats and assumptions

The visible report merges findings and implications section-by-section so a reader can audit the conclusion without reading code.

## Data Sources

- Fjelstul World Cup Database CSVs:
  - `tournaments.csv`
  - `qualified_teams.csv`
  - `matches.csv`
  - `tournament_standings.csv`
- FIFA API endpoints:
  - `https://api.fifa.com/api/v3/rankingschedules/all?type=0&gender=1&language=en`
  - `https://api.fifa.com/api/v3/rankingsbyschedule?rankingScheduleId={id}&language=en`

## Derived Artifacts

- `data/derived/ccs_team_year.csv`: team-year CCS flags for World Cups through 2022.
- `data/derived/fifa_rankings_pre_wc.csv`: nearest pre-tournament ranking snapshot for 1998-2026.
- `data/derived/favorite_traps.csv`: Top-20 FIFA-ranked teams that were not CCS candidates at tournament start; retained as the full audit table.
- `data/derived/favorite_trap_headliners.csv`: each tournament's single highest-ranked non-CCS team; retained as a sensitivity/reference table.
- `data/derived/favorite_trap_powerhouses.csv`: curated subset of the Top-20 non-CCS audit pool, focused on recognizable strong teams/title-relevant football brands for the main favorite-downgrade exhibit.
- `data/derived/ccs_2026_downgrade_giants.csv`: 2026 rank-strong, reputation-strong non-CCS teams highlighted in the live application section.
- `data/derived/random_benchmark.csv`: random same-size candidate-pool benchmark.
- `data/derived/contender_permutation_detail.csv`: year-level strong-team-label permutation inputs and champion hit probabilities.
- `data/derived/contender_permutation_summary.csv`: exact and simulated tail probabilities for the contender-label permutation benchmark.
- `data/derived/ccs_2026_watchlist.csv`: 2026 ranked teams with CCS status.

## Simulation Method

The contender-label permutation benchmark asks whether CCS is merely selecting famous football powers. For each tournament from 1998 to 2022, teams are split into a curated traditional title-contender set and all other teams. The simulation preserves how many CCS labels appear in each split, then randomizes labels within the split. This keeps the broad strong-team mix intact while testing whether the specific champion-chain label carries additional information.

France 1998 remains a documented no-prior-history exception and is excluded from the evaluable 6-of-6 simulation headline.

## Visualization QA

Charts are static PNGs generated by `scripts/build_report.py` with Matplotlib. Main charts are generated separately for English and Chinese so labels fit their report language. Each chart has an adjacent interpretation paragraph in the HTML report. The report was rendered to PDF with Chrome headless and inspected through PDF metadata/text extraction.
